services:
  backend:
    container_name: gatemind-backend
    build: .
    restart: unless-stopped
    env_file:
      - /opt/gatemind/backend.env
    environment:
      PORT: "5000"
      GUNICORN_WORKERS: "1"
      GUNICORN_TIMEOUT: "300"
    ports:
      - "80:5000"
    volumes:
      - /opt/gatemind/uploads:/app/uploads
